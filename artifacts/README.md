# Artifacts

Локальные артефакты ручных прогонов и диагностики (stress JSON, видео, временные кадры)
складываются в `artifacts/local/` и не коммитятся в репозиторий.

Для стресс-билда используется отдельный артефакт `dist/playable.stress.html`.
