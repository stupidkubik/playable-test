{
  "name": "playable-runner-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "extract:assets": "node scripts/extract-assets.mjs",
    "build": "node scripts/extract-assets.mjs && node scripts/build-single-html.mjs",
    "build:pages": "npm run build && node scripts/prepare-pages.mjs"
  },
  "dependencies": {
    "pixi.js": "^8.16.0"
  }
}
